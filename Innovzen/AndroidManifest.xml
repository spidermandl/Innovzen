<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.innovzen.o2chair"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />
    
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />    
    <uses-permission android:name="android.permission.MOUNT_UMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <application
        android:name="com.innovzen.application.InnovzenApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        

        <!-- SPLASH SCREEN -->
        <activity
            android:name="com.innovzen.activities.ActivitySplashScreen"
            android:label="@string/app_name" >
               <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LOGIN -->
        <activity
            android:name="com.innovzen.activities.ActivityLogin"
            android:label="@string/app_name" >
           
        </activity>

        <!-- MAIN -->
        <activity
            android:name="com.innovzen.activities.ActivityMain"
            android:label="@string/app_name" >
            
        </activity>
        <!-- test -->
        <activity
            android:name="com.innovzen.activities.ActivityTest"
            android:label="@string/app_name" >
          
        </activity>
    </application>

</manifest>